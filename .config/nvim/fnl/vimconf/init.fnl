; I'm currently testing fennel out. 
;For the time being it's just loaded, but the intention is to move a large portion of post-plugin configuration here.
(module vimconf.init
  {require {nvim aniseed.nvim}})