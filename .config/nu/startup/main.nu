# Note: nushell currently uses pwd to resolve sourcing, so we use absolute paths. 
source ~/.config/nu/startup/aliases/main.nu
# Note: for most environment variables, I currently use the profile from zsh.
# If this becomes my main shell, I may move the profile dir, but that isn't likely. 